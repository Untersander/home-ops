# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/kustomization-kustomize-v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: opencloud
  namespace: cloud
spec:
  components:
    - ../../../components/backup-kopiur
  postBuild:
    substitute:
      APP: opencloud
  # kopiur's webhook rejects policies while the ClusterRepository doesn't exist,
  # and the auto-restore PVCs must not be created before the repository is there
  dependsOn:
    - name: backup-repositories
      namespace: backup
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./apps/cloud/opencloud
  targetNamespace: cloud
  prune: true
  interval: 1h
