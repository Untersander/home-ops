# Paperless data (documents/, media.lock, consume/, export/) lives next to the media folder, outside of Jellyfin's volume
apiVersion: v1
kind: PersistentVolume
metadata:
  name: paperless-media
spec:
  capacity:
    storage: 200Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /var/mnt/zpool/paperless
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - cp-0
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-media
  namespace: paperless-ngx
  labels:
    backup.k8s.garden/app: paperless-ngx
    backup.k8s.garden/copy-method: Direct # openebs-hostpath-ssd sc (cluster default) has no CSI snapshots
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
  storageClassName: "local-storage"
  volumeName: paperless-media